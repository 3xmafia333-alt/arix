{
  "name": "cdn-metrics-pipeline",
  "version": "3.1.0",
  "description": "Edge-native CDN asset pipeline and layout rendering service",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "lint": "echo 'No lint step configured'"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
